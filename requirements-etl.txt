# LEGACY — dependencies are now managed in pyproject.toml
# Install with: pip install -e .
# Or with GPU support: pip install -e ".[gpu]"
# Or with LLM support: pip install -e ".[llm]"
# Or everything: pip install -e ".[all]"
#
# This file is kept for backward compatibility with existing docs.
vaderSentiment>=3.3.2
NRCLex==3.0.0
rapidfuzz>=3.0.0
