# Byte-compiled / optimized
__pycache__/
*.pyc
*.pyo

# Data files (too large for git)
*.db
*.db-wal
*.db-shm
*.csv
*.jsonl
*.json
*.accdb
*.zip
*.gz

# Environment
.env

# The web app has its own repo (UFOSINT/ufo-explorer)
ufo-explorer/

# Source data directories
UFOCAT/
NUFORC/
MUFON/
UPDB.app/
Geldreich/
UAP-Check/

# GeoNames gazetteer data (downloaded at build time)
geodata/

# Shared data root lives at ../data (UFOSINT/data/).
# Covers both a real folder and a symlink named `data` inside this repo.
/data
/data/

# Build / run logs
*.log

# Misc
nul
*.png
