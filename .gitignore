# Byte-compiled / optimized
__pycache__/
*.pyc
*.pyo

# Data files (too large for git) — README.md files are tracked anywhere
*.db
*.db-wal
*.db-shm
*.csv
*.jsonl
*.json
*.accdb
*.zip
*.gz
*.parquet

# Environment
.env

# The legacy in-repo Flask GUI prototype (replaced by ufosint-explorer)
ufo-explorer/

# GeoNames gazetteer data (downloaded at build time, ~10 MB extracted)
geodata/

# Build / run logs
*.log

# Misc
nul
*.png
